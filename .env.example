# 复制为 .env.local (开发) 或 .env (部署)
DATABASE_URL="file:./data/app.db"
# APP_SECRET=          # 生产环境必填，AES-256 加密密钥
# APP_PORT=3000
# CLOUDFLARE_TUNNEL_TOKEN=  # Cloudflare Tunnel token (Dashboard 获取)
# CLOUDFLARE_TUNNEL_URL=    # 公网域名，如 https://cycling.example.com
